const __dirname = new URL('.', import.meta.url).pathname;

export const pathMedia = __dirname + '../out/media/';
export const pathDeck = __dirname + '../out/deck/';
export const pathBuild = __dirname + '../out/';
